# 정보보안기사 실기 — 지문 보충 / 문제 복원 정리

> 최종 정리: 2026-09-10. 이전 작업목록(원본 지문 수집용 워크시트, ~566줄)은 반영이 끝나 이 문서로 대체.

## 요약

원본 실기 JSON(`실기/N회/N.json`)이 **paraphrase·재구성본**이라, 실제 기출 지문·문항을 확보한
문항은 `meta/N.json` 레이어에서 **문제 본문(`question`)·정답(`answer`)을 교체**한다.
(`실기/` 원본은 여전히 불변 — 절대 규칙 1.)

| 구분 | 건수 | 처리 |
|---|---|---|
| 실제 기출 지문 확보 → 문제 본문에 병합 | 29 | `meta.question` 로 교체, `supplement`/`supplementSrc` 삭제. UI에 "복원/재구성" 표식 없음 |
| 실제 기출 지문(이미지) | 1 | 10회 15번 — `meta.supplement` + `supplementSrc` 유지(`![](img/r10q15.jpg)`), `.supplement.attached` 로 표식 없이 문제에 이어 표시 |
| 실제 기출 문항 확보 → 문제 교체 | 10 | 31회 1·2·3·5·6·7·10·11·12·16 (`meta.question`, 5번은 `answer` 도 교체) |
| 정답 기반 재구성(원본 미확보) | 1 | 5회 14번 — `meta.supplement`(src 없음) + "🧩 지문 재구성" 캡션 유지 |

빌드 로그: `문제/정답 교체 39건 · 보충 지문 2건`.

## meta/N.json 스키마 (교체 관련)

```json
"7": {
  "domain": "...", "explanation": "...",
  "question": "실제 기출 문항 전문 (지문·보기·코드 포함, plain text — app 이 escape 렌더)",
  "answer": "형식이 바뀌어 정답 표기도 맞춰야 할 때만"
}
```

- `question` 이 있으면 `실기/` 원본 대신 이 값을 쓴다(`build.mjs applyMeta`, `overridden` 카운트).
- 코드·로그·HTML 이 들어가도 `q.question` 은 `esc()` 로 렌더되므로 코드펜스 불필요.
- 이미지 지문만 예외: `supplement` + `supplementSrc` 로 두고 `![alt](img/rNqM.jpg)`
  (`docs/img/` 에 파일, `docs/sw.js` SHELL 등록). `supplementHtml()` 이 `.supplement.attached` 로 표식 없이 렌더.
- `supplementSrc` 없는 `supplement` → "🧩 지문 재구성" 캡션(원본 미확보 명시). 현재 5회14 하나.

## 병합된 29문항

5회16 · 7회15·16 · 10회2·4·6·7·10 · 11회2·3·7·8·9·11·13·15 · 12회2·3·4·5·7·8·9·10·14 · 31회9·17·18 · 32회17

## 남은 것

- **5회 14번**: "악성코드 시스템 변경 분석" 지문 — 실제 기출 미확보. 재구성본 유지(캡션으로 명시). 확보 시 `meta.question` 으로 교체.
- **2회 15번 / 3회 12번**: 지시문이 산문으로 상황을 충분히 설명해 지금도 풀 수 있음. 실제 캡처/로그 확보 시 병합.
- **31회 5번 정답**: 문항 형식이 ①② 로 바뀌어 `answer` 를 `① HTTPERR / ② DHCP` 로 잠정 기입 — 검증 필요.
- 나머지 회차: 원본이 자체 완결이라 교체 불필요(누락 아님).
