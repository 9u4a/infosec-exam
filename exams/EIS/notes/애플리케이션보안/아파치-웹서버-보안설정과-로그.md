---
title: Apache 웹서버 보안 설정 · 로그 분석
domain: 애플리케이션보안
questions: [5-15, 7-15, 13-15, 14-8, 14-15, 17-14, 17-16, 19-7, 20-8, 21-10, 24-10, 29-11]
tags: [httpd.conf, Options, indexes, FollowSymLinks, LimitRequestBody, Access로그, Error로그]
---

## 설정 파일

- `httpd.conf` (또는 `apache2.conf`) — 메인 설정. `Include conf.d/*.conf`.
- `.htaccess` — 디렉터리별 설정 (`AllowOverride` 로 허용 범위 제어).

## `<Directory>` 보안 지시자 (17-16, 20-8, 29-11)

```apache
<Directory /var/www>
    Options Indexes FollowSymLinks     # ← 위험
    AllowOverride None
    Require all granted
</Directory>
```

| 지시자 | 위험 | 조치 |
|---|---|---|
| **Indexes** | 인덱스 파일 없을 때 **디렉터리 목록 노출** (Directory Listing/Indexing) | `Options -Indexes` (또는 Indexes 삭제) |
| **FollowSymLinks** | 심볼릭 링크로 웹 루트 밖(`/etc/passwd` 등) 접근 | 제거 또는 `SymLinksIfOwnerMatch` |
| **Includes** | SSI(`<!--#exec-->`) 명령 실행 | `-Includes` 또는 `IncludesNOEXEC` |
| **AllowOverride All** | `.htaccess` 로 설정 우회 | `None` |

> 디렉터리 인덱싱 판단(13-15, 5-15): 응답 **200 OK** + 본문에 `Index of /경로` 목록 → 성공.

## 주요 옵션 (14-15)

| 지시자 | 의미 |
|---|---|
| `Timeout 300` | 300초 동안 통신 없으면 연결 종료 (Slowloris 대응은 더 짧게) |
| `KeepAlive On` / `MaxKeepAliveRequests 100` | 커넥션 재사용, 커넥션당 최대 요청 수 |
| `KeepAliveTimeout 5` | KeepAlive 유지 시간 |
| `DirectoryIndex index.html index.php` | 디렉터리 접근 시 찾을 기본 파일·순서 |
| **`LimitRequestBody <바이트>`** | 요청 본문(업로드) 최대 크기 제한 (14-8, 21-10) |
| `LimitRequestFields`, `LimitRequestLine` | 헤더 개수·요청라인 길이 제한 (버퍼 오버플로 대응) |
| `ServerTokens Prod` / `ServerSignature Off` | 버전 배너 숨김 |
| `TraceEnable Off` | TRACE 메서드 차단 (XST 대응) |

## HTTP 메서드 제한 (LimitExcept)

```apache
# GET, POST 외 모든 메서드(PUT/DELETE/TRACE/CONNECT 등) 차단
<LimitExcept GET POST>
    Order allow,deny
    Deny from all
</LimitExcept>
```
확인: `telnet webserver.com 80` 후 `OPTIONS / HTTP/1.0` 전송 → 응답의 `Allow:` 헤더에 TRACE 등이 보이면 취약(배너 그래빙으로도 서버 종류·버전 노출 확인 가능).

## 접근 제어 (Order / Deny / Allow)

```apache
# 전체 차단 후 관리자 IP만 허용
<Directory /var/www/admin>
    Order Deny,Allow
    Deny from all
    Allow from 192.168.56.30
</Directory>
# 백업·설정 파일 확장자 접근 차단
<Files "*.bak">
    Order Allow,Deny
    Deny from all
</Files>
```
`Order Deny,Allow`는 **Deny를 먼저 적용한 뒤 Allow로 예외**를 두는 방식(기본 거부), `Order Allow,Deny`는 그 반대(기본 허용) — 순서가 뒤바뀌면 의도와 반대로 동작하므로 서술형에서 반드시 순서를 명시한다.

## ServerTokens 노출 수준 비교

| 설정값 | 응답 헤더 예 | 보안 수준 |
|---|---|---|
| `Full` | `Apache/2.4.6 (CentOS) OpenSSL/1.0.1` | 매우 취약(OS·모듈 버전까지 노출) |
| `OS` | `Apache/2.4.6 (CentOS)` | 취약 |
| `Min`/`Minimal` | `Apache/2.4.6` | 보통 |
| `Prod`/`ProductOnly` | `Apache` | ✅ 권장(최소 노출) |

## PHP 보안 설정 (`/etc/php.ini`)

| 항목 | 안전한 값 | 설명 |
|---|---|---|
| `display_errors` | `Off` | 에러 메시지 화면 노출 차단(경로·쿼리 등 정보 유출 방지) |
| `allow_url_fopen` | `Off` | RFI(원격 파일 포함) 방어 |
| `session.cookie_httponly` | `1` | 세션 쿠키 HttpOnly — XSS의 쿠키 탈취 방어 |
| `session.cookie_secure` | `1` | 세션 쿠키를 HTTPS 구간에서만 전송 |
| `session.gc_maxlifetime` | 예: `600` | 세션 유효 시간(초) |

## 로그 (19-7)

| 로그 | 지시자 | 내용 |
|---|---|---|
| **Access 로그** | `CustomLog logs/access_log combined` | 정상 접속 (IP, 시각, 요청라인, 상태코드, 크기, Referer, User-Agent) |
| **Error 로그** | `ErrorLog logs/error_log` | 처리 오류, 모듈 경고 |

경로 확인 파일: **`httpd.conf`** 의 `CustomLog` / `ErrorLog` 지시자.

### Combined 로그 형식 (24-10)

```
200.3.1.4 - - [30/May/2023:01:20:01 +0900] "GET /bulletin/read.php?no=101&item=book HTTP/1.1" 200 3549 "http://ref" "Mozilla/5.0"
```

| 순서 | 필드 | 예시 값 |
|---|---|---|
| 1 | IP | `200.3.1.4` |
| 2 | ident | `-` |
| 3 | 인증 사용자 | `-` |
| 4 | 시각 | `[30/May/2023:01:20:01 +0900]` |
| 5 | 메서드·URL·프로토콜 | `"GET /bulletin/read.php?no=101&item=book HTTP/1.1"` |
| 6 | **상태코드** | `200` |
| 7 | 응답 크기 | `3549` |
| 8 | Referer | `"http://ref"` |
| 9 | User-Agent | `"Mozilla/5.0"` |

### 공격 로그 판별

- **7-15 HTTP Request 문제점**
  1. `id`·`password` 가 **GET 파라미터 평문**(URL·로그·Referer에 노출) → POST + HTTPS로 개선
  2. `Cache-Control: must-revalidate` 로 캐시 미사용 → 원본 서버 부하
  3. 세션 쿠키 평문 구간 노출
- **17-14 유니코드 취약점**: `GET /script/..%c1%1c../winnt/system32/cmd.exe?/c+dir` → 디렉터리 트래버설(유니코드 인코딩) 통한 RCE 시도. 응답 **404** = 실패(패치됨). 대응: 웹서버 패치, 입력 필터링, 웹루트 분리, IPS 룰.
- 상태코드 빠른 해석: 200 성공 / 301·302 리다이렉트 / 403 금지 / 404 없음 / 500 서버오류. 공격 성공 여부는 대개 **200 vs 403/404** 로 판단.
